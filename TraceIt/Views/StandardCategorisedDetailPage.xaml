<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:xforms="clr-namespace:Syncfusion.ListView.XForms;assembly=Syncfusion.SfListView.XForms" 
             xmlns:buttons="clr-namespace:Syncfusion.XForms.Buttons;assembly=Syncfusion.Buttons.XForms"
             xmlns:viewmodels="clr-namespace:TraceIt.ViewModels"
             xmlns:triggers="clr-namespace:TraceIt.Triggers"
             x:Class="TraceIt.Views.StandardCategorisedDetailPage"
             x:Name="myLayout"
             BackgroundColor="Black">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Style TargetType="Label">
                <Setter Property="TextColor" Value="White"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.Content>
        <StackLayout Style="{DynamicResource gradientStackLayout}">

            <xforms:SfListView x:Name="categorisedStandardsList" VerticalOptions="Fill" ItemSize="80"
                               ItemTapped="categorisedStandardsList_ItemTapped" SelectionMode="None"
                               ItemSpacing="10" AutoFitMode="Height" Padding="25">
                <xforms:SfListView.ItemTemplate>
                    <DataTemplate>
                        <Frame Style="{DynamicResource listItemFrame}">
                            <Grid RowDefinitions="*, *" ColumnDefinitions="2*, 1*">
                                <Label Text="{Binding Code}" FontAttributes="Bold" Grid.Column="0" Grid.Row="0"/>
                                <Label Text="{Binding Title}" Grid.Column="0" Grid.Row="1"/>

                                <Button x:Name="selectionButton" Text="Add" BackgroundColor="Yellow" 
                                        TextColor="Black" FontAttributes="Bold"
                                        Grid.Column="1"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:StandardCategorisedDetailPageViewModel}}, Path=ChangeStandardSelectionCommand}" 
                                        CommandParameter="{Binding Source={x:Reference selectionButton}, Path=BindingContext}">
                                    <Button.Triggers>
                                        <DataTrigger TargetType="Button" Binding="{Binding Selected}"
                                                     Value="true">
                                            <DataTrigger.EnterActions>
                                                <triggers:SelectedTriggerAction/>
                                            </DataTrigger.EnterActions>

                                            <DataTrigger.ExitActions>
                                                <triggers:DeselectedTriggerAction/>
                                            </DataTrigger.ExitActions>
                                        </DataTrigger>
                                    </Button.Triggers>
                                </Button>
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </xforms:SfListView.ItemTemplate>
            </xforms:SfListView>

        </StackLayout>
    </ContentPage.Content>

</ContentPage>