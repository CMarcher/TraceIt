<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:Button="clr-namespace:Syncfusion.XForms.Buttons;assembly=Syncfusion.Buttons.XForms" 
             xmlns:xforms="clr-namespace:Syncfusion.ListView.XForms;assembly=Syncfusion.SfListView.XForms"
             mc:Ignorable="d"
             x:Class="TraceIt.Views.SubjectsPage"
             Title="Subjects"
             BackgroundColor="Black">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="+"/>
        <ToolbarItem Text="Edit"/>
    </ContentPage.ToolbarItems>

    <ContentPage.Resources>
        <Style TargetType="Label">
            <Setter Property="TextColor" Value="White"/>
            <Setter Property="HorizontalTextAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="17"/>
        </Style>

    </ContentPage.Resources>

    <ContentPage.Content>
        <AbsoluteLayout Style="{DynamicResource gradientAbsoluteLayout}">

            <StackLayout Padding="14, 14, 14, 0" AbsoluteLayout.LayoutFlags="All"
                         AbsoluteLayout.LayoutBounds="0, 0, 1, 1">
                <xforms:SfListView x:Name="collectionViewSubjects" ItemSize="180" ItemSpacing="20"
                                   SelectionMode="None">

                    <xforms:SfListView.LayoutManager>
                        <xforms:GridLayout SpanCount="2"/>
                    </xforms:SfListView.LayoutManager>

                    <xforms:SfListView.ItemTemplate>
                        <DataTemplate>
                            <Frame Style="{DynamicResource listItemFrame}" Padding="0">
                                <AbsoluteLayout HeightRequest="180">

                                    <Label Text="{Binding Name}"
                                           AbsoluteLayout.LayoutFlags="All"
                                           AbsoluteLayout.LayoutBounds="0.5, 0.1, 0.9, 0.2"
                                           FontAttributes="Bold"/>

                                    <Label Text="0 / 0 credits"
                                           AbsoluteLayout.LayoutFlags="All"
                                           AbsoluteLayout.LayoutBounds="0.5, 0.35, 0.9, 0.2"/>

                                    <Label Text="0 standards"
                                           AbsoluteLayout.LayoutFlags="All"
                                           AbsoluteLayout.LayoutBounds="0.5, 0.53, 0.9, 0.2"/>

                                    <Button:SfButton Text="View Info" CornerRadius="30"
                                                     TextColor="Black"
                                                     FontAttributes="Bold"
                                                     FontSize="17"
                                                     AbsoluteLayout.LayoutFlags="All"
                                                     AbsoluteLayout.LayoutBounds="0.5, 0.85, 0.7, 0.2"
                                                     Style="{DynamicResource subjectButtonGradient}"
                                                     Clicked="buttonViewInfo_Clicked"/>

                                </AbsoluteLayout>
                            </Frame>
                        </DataTemplate>
                    </xforms:SfListView.ItemTemplate>
                    
                    

                </xforms:SfListView>
            </StackLayout>
        </AbsoluteLayout>
    </ContentPage.Content>
</ContentPage>